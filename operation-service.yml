apiVersion: v1
kind: Service
metadata:
  name: operation-service
  namespace: app-namespace
spec:
  selector:
    app: operation-app
  ports:
    - name: http
      protocol: TCP
      port: 80